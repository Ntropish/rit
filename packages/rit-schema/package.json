{
  "name": "@rit/schema",
  "version": "0.0.4",
  "description": "Entity schema and typed CRUD layer for rit",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@rit/core": "^0.0.3"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
